services:
  dev:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app:Z
    command: >
      sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 8080"
    environment:
      - NODE_ENV=development
